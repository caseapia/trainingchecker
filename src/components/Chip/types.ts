interface Props {
  label: string;
  className?: string;
  size?: 'small' | 'medium' | 'large';
  icon?: React.ReactNode;
  iconPosition?: 'left' | 'right';
  iconSize?: number;
  image?: string;
  color?: string;
}